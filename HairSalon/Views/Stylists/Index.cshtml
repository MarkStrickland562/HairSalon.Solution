<h1>Curl Up and Dye Hair Salon</h1>
<hr />
<h2>Stylists:</h2>

@if (Model.Count != 0)
{
  <ul>
    @foreach (var stylist in Model)
    {
      <li><a href="/Stylists/@stylist.GetId()">@stylist.GetName()</a></li>
    }
  </ul>
}

<h4><a href="/Stylists/new">Add a New Stylist</a></h4>
<h4><a href="/">Main Page</a></h4>
