@{
  Layout = "_Layout";
}

<h1>Curl Up and Dye Hair Salon</h1>
<h2>Clients:</h2>

@if (Model.Count != 0)
{
  <ul>
    @foreach (var client in Model)
    {
      <li><a href='/clients/@item.GetId()'>@item.GetName()</a></li>
    }
  </ul>
}

<h4><a href="/clients/new">Add a New Client</a></h4>
<h4><a href="/">Main Page</a></h4>
